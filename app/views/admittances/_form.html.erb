<%= form_for(@admittance) do |f| %>
  <% if @admittance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admittance.errors.count, "error") %> prohibited this admittance from being saved:</h2>

      <ul>
      <% @admittance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :access_to %><br>
    <%= f.text_field :access_to %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
